import { SettingsView } from '@/components/admin/SettingsView';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'CONFIGURACIÓN · FASHION KAT',
};

export default function AdminConfiguracionPage() {
  return <SettingsView />;
}
